# launchfile

This package has moved to [`@launchfile/sdk`](https://www.npmjs.com/package/@launchfile/sdk).

```bash
npm install @launchfile/sdk
```

See [launchfile.org](https://launchfile.org) for the specification and [launchfile.dev](https://launchfile.dev) for developer docs.
