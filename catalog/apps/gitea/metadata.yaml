category: Git
tagline: "Lightweight self-hosted Git service"
homepage: https://about.gitea.com
test_results:
  last_tested: 2026-09-24
  pull_time_seconds: 17
  startup_time_seconds: 9
  total_disk_mb: 646
  health_check_passed: true
  notes: >-
    2026-09-25, catalog/test harness at 6705c17: GITEA__database__HOST resolved to gitea-postgres:5432.
    The harness healthcheck passes on the install page, before Gitea opens a database connection
    (INSTALL_LOCK=false). Submitting Gitea's install form with its prefilled values unchanged made Gitea
    connect: log "PING DATABASE postgres", no dial or connection-refused errors; Postgres then held
    116 tables and idle sessions from the gitea container.
images:
  - name: gitea/gitea:latest
    size_mb: 254
    platform:
      - linux/arm64
  - name: postgres:16-alpine
    size_mb: 392
    platform:
      - linux/arm64
