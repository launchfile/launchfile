---
import DocsLayout from "@/layouts/DocsLayout.astro";
import { getEntry, render } from "astro:content";

const entry = await getEntry("sdk", "readme");
if (!entry) throw new Error("SDK README.md not found in sdk collection");
const { Content } = await render(entry);
---

<DocsLayout title="SDK Reference — Launchfile" description="TypeScript SDK for parsing, validating, and serializing Launchfiles." currentPath="/sdk/">
  <Content />
</DocsLayout>
